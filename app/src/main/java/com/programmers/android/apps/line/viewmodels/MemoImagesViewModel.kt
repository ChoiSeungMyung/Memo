package com.programmers.android.apps.line.viewmodels

import android.app.Application
import androidx.lifecycle.AndroidViewModel
import androidx.lifecycle.LiveData
import com.programmers.android.apps.line.adapters.MemoImagesAdapter
import com.programmers.android.apps.line.models.MemoImagesRepository

class MemoImagesViewModel(application: Application) : AndroidViewModel(application) {
//    private val repository: MemoImagesRepository
//    val allImages: ArrayList<String?>
//
//    init {
//        repository = MemoImagesRepository.getInstance(adapter)
//        allImages = repository.getAllImages()
//    }
//
//    fun insertImage(path: String?) = repository.insertImage(path)
//    fun deleteImage(index: Int) = repository.deleteImage(index)
//    fun getImage(index: Int) = repository.getImage(index)
}